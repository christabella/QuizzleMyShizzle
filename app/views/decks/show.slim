.container
  .col-md-12
    h1
      = @deck.name
      small
        | Cards: #{@deck.cards.count}

    - if @deck.cards.count > 0
      = link_to deck_card_path(@deck, @deck.cards.order("next_repetition DESC").first)
        button.btn.btn-success Start Quiz
